// place files you want to import through the `$lib` alias in this folder.
export {
	isValidScore,
	normalizeName,
	NAME_MAX_LENGTH,
	NAME_MIN_LENGTH,
	MAX_SCORE,
	MIN_SCORE,
	SCORE_STEP,
	NAME_PATTERN,
} from './shared/validation'
