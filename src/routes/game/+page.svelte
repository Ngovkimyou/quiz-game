<script lang="ts">
	// import { UpdateScore } from '$lib/components/updateScore';

	// Just call doUpdate(name, score) you don't need to do anything else
	// if the user is first time, then they will be register in the database
	// if the user already register, they will update their score instead
	// I made the UpdateScore function to store their name and score in the cookie

	// function doUpdate(id: string, score: number) {
	// 	UpdateScore(id, score);
	// }

	// Uncomment below to get the name and score in the cookies
	// const {data} = $props();
	// const name = data.name
	// const score = data.score
</script>

Hello
